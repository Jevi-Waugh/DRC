#include <stdio.h>
#include<stdlib.h>
#include<string.h>
#include <wiringPi.h>
#include "pid.h"

int main(int argc, char* argv[]) {
    setup_gpio();

    // Example usage
    // need an if statement for all use cases
    //maybe use strcm for string check for whether to move wherever
    //use sensors

    // Clean up (optional, wiringPi does not have an explicit cleanup function)
    struct pid = 
    return 0;
}